<%= form_with(model: distribution_center, local: true) do |form| %>
  <% if distribution_center.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(distribution_center.errors.count, "error") %> prohibited this distribution_center from being saved:</h2>

      <ul>
        <% distribution_center.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
