<%= form_with(model: fulfillment_center, local: true) do |form| %>
  <% if fulfillment_center.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fulfillment_center.errors.count, "error") %> prohibited this fulfillment_center from being saved:</h2>

      <ul>
        <% fulfillment_center.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
